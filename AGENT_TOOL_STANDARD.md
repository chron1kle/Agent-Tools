# Agent Tools 架构规范

> **版本**: 2.0
> **定位**: 项目级架构设计文档
> **适用**: Agent Tools 项目整体架构、组件设计

---

## 一、项目愿景

建立一套标准化的 AI Agent 工具开发框架，实现：

| 目标 | 说明 |
|------|------|
| **统一入口** | 所有工具通过 `src/__init__.py` 提供一致的调用接口 |
| **单例模式** | 工具进程由 Claude 精确控制生命周期 |
| **多语言支持** | Python / Node.js / Go / Rust 等实现可共存 |
| **MCP 集成** | 标准化 MCP 协议接口，LLM 可直接调用 |
| **可观测** | 统一的日志系统 |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Claude Desktop                         │
│                         (LLM)                               │
└─────────────────────────┬───────────────────────────────────┘
                          │ MCP
┌─────────────────────────▼───────────────────────────────────┐
│                   Tool MCP Server                           │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ __init__.py (单例模式)                               │  │
│  │ - ToolManager: 生命周期管理                           │  │
│  │ - TOOL_SCHEMA: 参数定义                             │  │
│  │ - MCP Server: 协议转换                               │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    Tool Implementation                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│  │ main.py   │  │index.js  │  │ main.go  │               │
│  │(Python)   │  │(Node.js) │  │  (Go)    │               │
│  └──────────┘  └──────────┘  └──────────┘               │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、核心组件

### 3.1 工具模板 (demo-tool/)

```
demo-tool/
├── src/
│   └── __init__.py         # 统一入口（含 MCP + 单例）
├── config.json             # 配置文件
├── README.md              # 简要文档
└── manual.md              # 详细文档
```

### 3.2 公共组件 (common/)

| 组件 | 路径 | 说明 |
|------|------|------|
| Logger | `common/logger/python/` | TCP 日志广播 |
| Config Env Reg | `common/conf-env-reg/python/` | 配置注册为环境变量 |

---

## 四、关键设计决策

### 4.1 统一入口

所有工具通过 `src/__init__.py` 提供：

```python
from tool.src import run, ToolManager
```

### 4.2 单例模式

- 工具进程启动后持续运行
- Claude 通过 MCP 管理生命周期：`--start/--stop/--status/--restart`

### 4.3 MCP 为主要接口

```
生产环境 ──→ MCP 模式 ──→ ToolManager ──→ Tool
开发调试 ──→ 命令行 ──→ 直接调用
```

### 4.4 多语言共存

```
src/
├── main.py      # Python 实现
├── index.js     # Node.js 实现
└── main.go      # Go 实现
```

`__init__.py` 按优先级自动检测可用实现。

---

## 五、工具创建流程

```
1. 复制 demo-tool/
       │
       ▼
2. 重命名目录，替换占位符
       │
       ▼
3. 实现工具逻辑
       │
       ▼
4. 配置 TOOL_SCHEMA
       │
       ▼
5. 在 config.json 声明 MCP
```

详见 `demo-tool/` 模板目录。

---

## 六、版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 2.0 | 2026-05-05 | 引入 MCP + 单例模式 |
| 1.2 | 2026-05-04 | 初始版本 |

---

**最后更新**: 2026-05-05
